package ex_13_Functions;

import java.lang.Math;
//by default java is loaded with certain build in functions like math.max,.....
//these can be used by using this
//we can use without adding this also - as this is added by default even without displaying it

public class Lab128_Functions {
    public static void main(String[] args) {
        int max= Math.max(3,4);
        System.out.println(max);
    }
}
